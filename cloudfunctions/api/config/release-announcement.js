/**
 * 版本更新广播：
 * 1. 每次发布时修改 id、version、title 和 content。
 * 2. enabled=true 后，用户下次打开“日历”或“今日”页时会收到一次消息中心通知。
 * 3. 同一个 id 不会重复发送；若要重新广播，必须更换 id。
 */
module.exports = Object.freeze({
  enabled: true,
  id: '2026-09-18-social-encouragement',
  version: '1.5.0',
  title: '打卡提醒与特别关心上线',
  content: '完成绑定计划后会自动同步群组打卡动态，群成员可以点赞鼓励。好友可设为特别关心，即使不在同一群也能在圈子看到打卡；群组和特别关心都可主动订阅下一次微信提醒。误点完成可直接撤回。“我的”已精简为常用入口，消息中心从今日页右上角进入并只保留最近 20 条。'
})
