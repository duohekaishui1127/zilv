# Changelog

## 未发布

### 长期目标与执行任务
- 计划升级为“执行任务 / 长期目标”两层结构；旧计划自动兼容为执行任务。
- 长期目标支持日期倒计时、默认 21 天习惯养成、定量或无上限积累，进度由关联任务打卡自动汇总。
- 执行任务支持一次性日期和可选执行时间；任务可统一绑定长期目标与监督群组。
- 长期目标固定为私密数据，不进入好友日历、特别关心、群组绑定、群动态或打卡提醒。
- 今日卡片支持长按调整顺序或隐藏，并可通过长按空白处或“今日页设置”恢复。
- “我的”新增独立的“我的进步”卡片，集中保留已完成目标、习惯和考试历程，并支持点击查看详情。
- 习惯连续达标后先发送达成提醒再自动归档；考试目标在剩余 200、100、30、7、1 天时提醒，考试日自动归档且不进入逾期状态。
- 考试归档冻结创建日期、考试日期、准备天数、历史关联任务、完成次数、学习时长与完成率；结果可选择待出分、通过、未通过或未参加，分数和复盘选填。
- 待出分满 60 天时默认发送一次消息中心提醒；归档不会删除、停用或解除关联的短期任务。
- 长期目标提醒作为默认能力自动进入消息中心，不显示开关，也不弹出或发送微信订阅消息。
- 今日页考试目标改为醒目的倒计时徽标，并隐藏无关联任务时没有意义的“近7天 0/0”。
- 好友列表按用户去重；初始化迁移会把历史重复好友关系标记为 `DUPLICATE`，保留审计数据并停止重复展示。
- Schema 升级为 14，无需新增集合；旧计划无需批量回填，历史考试在首次读取时补齐归档快照。

### 置顶与群聊生命周期
- 好友和群聊新增置顶时间排序，新置顶对象会立即移动到列表首位，客户端同时执行一致性排序。
- 群主可在群详情二次确认后解散群聊；解散会移出所有成员、解除计划绑定、终止待审批变更并使旧邀请失效。
- 群主修改自己绑定到本群的计划时自动批准；若还绑定到其他人管理的群，仍只等待那些群主审批。变更生效后会写入相关群动态并向成员发送站内消息。

### 长期打卡提醒
- 计划提醒新增一次性/长期订阅模式；微信后台提供长期模板时，用户授权一次即可在后续应打卡日持续接收未完成提醒。
- 定时提醒和群组自动移出任务改为分页扫描，不再遗漏单次查询上限之外的计划与群组。

### 好友申请与打卡日历
- 群成员当月打卡页新增圆形“＋好友”入口，并区分申请中、待处理和已是好友状态。
- 好友申请支持最多 60 字的选填申请备注，备注会同时出现在好友申请列表与消息中心通知中。
- 好友资料由打卡流水升级为当月日历；点击日期只展示对方授权公开的任务名称和完成状态，不展示心情、备注或小记。
- 群成员日历与好友日历共用任务进度规则，避免两个入口出现统计差异。

## 1.6.0 - 2026-09-18

### 群组邀请与详情精简
- 圈子入口统一改名为“群组”，列表显示人数与群组 ID，并提供复制和邀请快捷操作。
- 支持从好友列表多选邀请，也支持输入个人 ID；邀请进入消息中心，对方确认后才加入，同一邀请每天去重。
- 群组详情移除邀请码卡片，“自律伙伴”移到顶部；打卡动态默认折叠，展开后显示时间戳并支持点赞。
- 群组成员改为未完成优先并按完成比例升序，已完成成员灰度沉底；点击成员可查看当月群组任务日历，严格排除心情、备注和小记。
- 微信提醒卡片移到详情底部，成员卡片移除重复标题和说明，仅保留群主身份提示。
- 微信社交提醒兼容长期与一次性模板：长期模板保持开关，一次性模板发送后仍按微信规则关闭；站内消息每次同步。

### 好友资料与申请闭环
- 好友申请增加消息中心通知、圈子红点、接受、拒绝与申请方撤回；同意或拒绝结果会通知申请人。
- 被拒绝或刚撤回的申请增加重新发送冷却，避免反复申请造成红点骚扰。
- 点击好友可进入资料页，按对方授予的权限查看最近打卡记录，不返回任务备注、整日心情或小记。
- 好友资料右上角集中管理仅自己可见的备注、特别关心、下一次微信提醒和删除好友。

### 两层好友隐私
- “我的 → 隐私设置”继续作为所有好友的统一默认规则。
- 新增单好友例外，可单独决定“我向 TA 公开”的内容；关闭例外后自动跟随全局设置。
- 特别关心动态和提醒统一经过单好友权限解析，不能绕过对方隐私。
- 新增 `friend_settings` 集合，Schema 升级为 9。

## 1.5.0 - 2026-09-18

### 社交鼓励与通知
- 群组继续以打卡动态为核心，不增加群聊；绑定计划完成后始终广播，成员可手动点赞鼓励。
- 群组新增“下一次微信打卡提醒”开关，按微信一次性订阅规则发送后自动关闭，不影响群内动态。
- 好友新增“特别关心”，即使不在同群，也能在圈子首页看到符合对方隐私设置的打卡动态，并可单独订阅下一次微信提醒。

### 撤回与一致性
- 点击已完成任务的勾选圆圈可直接撤回，仅闪现“已撤回”提示；点击任务内容仍可编辑备注。
- 撤回会同步取消当日自动打卡和日历印记，清理计划自动生成的学习/运动记录，并更新群动态。
- 重新完成会递增完成版本并恢复当日打卡；原心情和小记作为草稿保留。
- 新增 `group_event_likes`、`special_cares`，Schema 升级为 8。

### 我的与消息中心
- “我的”默认只显示我的历程、身体数据、趋势与复盘，其余低频入口收进当前页默认折叠的“其他”。
- 移除“我的”中的重复消息入口；今日页右上角以灰色图标和未读红点提供唯一常用入口。
- 未读消息高亮、已读消息变灰；每名用户只保留最近 20 条，版本公告与计划提醒使用独立投递凭证避免清理后重复发送。

## 1.4.0 - 2026-09-17

### 计划驱动的今日
- 底部导航调整为“日历、今日、计划、圈子、我的”，移除重复的“记录”入口并恢复核心圈子入口。
- 普通任务勾选即完成，计时任务结束即完成；单任务不再强制填写心情，完成后可按需补充备注。
- 完成全部今日计划后自动打卡并展示连续天数，再自动弹出整日心情与今日小记，支持稍后补充和再次编辑。

### 日历、身体与饮食
- 日历仅使用整日打卡心情作为当天印记，今日小记直接展示，完成任务保持默认折叠。
- 身体数据与定期更新提醒移至“我的”。
- 饮食改为带时间戳的进食时间流，不区分早中晚餐，支持食物营养、文字和最多 3 张照片。
- 新增 `daily_reviews` 集合和进食事件字段，Schema 升级为 7。

## 1.3.0 - 2026-09-17

### 自律日历与像素素材
- 新增默认日历首页，底部导航调整为“日历、今日、记录、计划、我的”，好友与群组移动到“我的”。
- 日历使用当天最后一次完成任务的心情作为黑白像素印记；无心情的打卡显示完成印记。
- 点击日期在当前页展示备注小记和图片；完成任务默认折叠，可手动展开查看计时、心情和备注。
- 原趋势页移除重复日历，保留身体、能量、运动与学习趋势。
- 新增可独立替换的 SVG 源素材与 PNG 运行素材，覆盖心情、底部导航、分类和计时操作图标。

### 计划专注计时
- 计划可选择不计时、正计时或倒计时，倒计时支持设置 1～1440 分钟。
- 今日页支持开始、暂停、继续与结束；切换页面或重新打开后按服务端时间恢复。
- 自动统计有效时间、总用时与暂停时间；倒计时达到目标后自动停止。
- 结束后可补充心情与备注，确认完成时同步到学习或运动分类记录。
- 同一时间仅允许一个计划处于计时或暂停状态，避免重复累计专注时间。
- Schema 升级为 6。

### 用户反馈与版本公告
- 新增意见与建议入口，支持分类、文字、最多 3 张截图和可选联系方式。
- 新增反馈管理员收件箱、处理状态及面向提交用户的状态通知。
- 新增独立版本公告配置，用户下次进入首页时会收到一次去重后的消息中心广播。
- 新增 `feedbacks` 集合，Schema 升级为 5。

## 1.2.0 - 2026-09-16

### 趋势与复盘
- 将“最近 7 天报告”升级为统一的“趋势与复盘”页面。
- 支持 7、30、90 天范围切换。
- 新增体重、体脂、预计热量平衡、运动与学习时长原生 Canvas 趋势图。
- 新增月度活跃日历，汇总饮食、运动、学习、身体记录和计划打卡。
- 新增活跃天数、运动/学习总时长、饮食记录天数、蛋白质达标和体重变化摘要。
- 饮食缺失日不再按零摄入计算热量缺口，避免产生误导性趋势。

### 性能与质量
- 报告数据改为服务端批量聚合，并分页读取最多 1000 条区间记录，避免云数据库默认条数导致漏算。
- 图表使用小程序原生 Canvas 2D，不增加第三方运行依赖。
- 新增日期跨月、聚合准确性、营养目标版本和活跃日历测试，自动测试增加到 14 项。
- 本版未修改数据库结构，Schema 继续保持 4。

## 1.1.0 - 2026-09-16

### 计划提醒
- 计划支持开启未打卡提醒、设置提醒时间和保存用户时区。
- 新增消息中心，展示未读状态与微信推送结果。
- 新增 `reminder-dispatch` 定时云函数，每 5 分钟检查到期且尚未完成的计划。
- 支持微信一次性订阅消息；发送成功或确认未授权后自动关闭该计划的微信推送标记。
- 使用“用户 + 计划 + 日期”生成幂等消息 ID，避免重复提醒。
- 每周次数计划达到本周目标后不再产生提醒。

### 数据与质量
- 新增 `notifications` 集合，Schema 升级为 4。
- 新增提醒时区、发送窗口和周范围测试，Domain 自动测试增加到 10 项。
- 静态质量门禁覆盖 `reminder-dispatch` 的版本与固定依赖。

## 1.0.0 - 2026-09-16

首个正式版本。

### 核心功能
- 支持身体数据、围度偏好、营养目标、饮食、运动、学习和周报。
- 支持每日、工作日、周末、指定星期与每周次数计划，以及手动/自动打卡。
- 支持好友、隐私设置、群组、计划绑定与群组监督事件。
- 支持日常、体态、学习、训练、饮食五类私密日志，最多 9 张图片。
- 支持日志与身体、学习、训练记录关联，以及时间轴查看、编辑、删除和草稿恢复。

### 架构与质量
- 将原社交 Action 拆分为 `privacy / friends / groups`，将活动模块拆分为 `workouts / study`，身体数据从 profile 中独立为 `body`。
- 新增统一输入校验 `lib/validators.js`。
- 新增 `domain/exercise-calculator.js`、`domain/note.js`，将易变化业务规则从数据库编排中隔离。
- 身体围度改为数据库定义 + 用户偏好驱动，不再在页面硬编码指标集合。
- 新增架构质量门禁：版本一致性、Schema 一致性、页面完整性、API Action 契约、固定依赖、Action 体量检查。
- 新增“关于与诊断”页面，展示客户端/服务端版本、Schema、环境和营养算法版本。
- Schema 升级为 3；初始化集合增加至 26 个。
- 新增 `body_metric_preferences`，用户可自行选择身体围度记录详细度。
- Domain 自动测试增加到 7 项。
