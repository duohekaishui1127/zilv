<view class="container">
  <view class="card warn-card"><view class="value">{{name}}</view><view class="sub">绑定后，计划名称和完成状态会在群内公开；本人担任群主的群自动批准计划变更，其他群仍需群主审核，生效后会向相关群成员广播。计划描述、备注和其他隐私内容不会公开。</view></view>
  <view class="card">
    <view class="list-item row" wx:for="{{groups}}" wx:key="_id">
      <view class="flex-1"><view class="value">{{item.displayName || item.name}}</view><view class="sub" wx:if="{{item.remark}}">群名称：{{item.name}}</view><view class="sub">{{item.memberCount}}人 · 邀请码 {{item.inviteCode}}</view></view>
      <button class="btn btn-small {{item.bound ? 'btn-danger' : ''}}" data-id="{{item._id}}" data-bound="{{item.bound}}" bindtap="toggle">{{item.bound ? '解除' : '绑定'}}</button>
    </view>
    <view class="empty compact" wx:if="{{!groups.length}}">还没有加入群组</view>
  </view>
</view>
